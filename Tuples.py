coordinate = (4,5) # tuples use (), they are inmutable. # tuple cannot change or modified.
print(coordinate[0]) # elepments in tuples can be grabbed using the [] using index.
# practically in python we use tuples for data which wil never be changed.
coordinate2 = [(4,5),(5,6),(8,9)] # these are tuples listed.
print(coordinate2[2][1]) # this is how we grab elements of listed tuples or nested lists using the index.

